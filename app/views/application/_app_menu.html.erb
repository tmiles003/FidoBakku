  <ul class="nav navbar-nav">
    <li>
      <a href="/#/dashboard" active-nav-link="active"><i class="fa fa-dashboard fa-lg"></i> Dashboard</a>
    </li>
  <% if can? :create, Evaluation # admins + managers %>
    <li>
      <a href="/#/admin/evaluations" active-nav-link="active"><i class="fa fa-file-text-o"></i> Evaluations</a>
    </li>
  <% end %>
  <% if can? :create, Form # admins + managers %>
    <li>
      <a href="/#/admin/forms" active-nav-link="active"><i class="fa fa-list-alt"></i> Forms</a>
    </li>
  <% end %>
  <% if can? :update, User # admins only %>
    <li>
      <a href="/#/admin/staff" active-nav-link="active"><i class="fa fa-users"></i> Staff</a>
    </li>
  <% end %>
  </ul>
  <ul class="nav navbar-nav navbar-right" ng-controller="CurrentUserCtrl">
    <li style="padding-top:6px;">
      <!-- use of gravatar directive here doesn't work, due to $promise -->
      <img ng-src="//secure.gravatar.com/avatar/{{ currentUser.email_hash }}?s=40&d=blank" alt="">
    </li>
    <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown">{{ currentUser.name }} <b class="caret"></b></a>
      <ul class="dropdown-menu fi-dropdown-menu">
      <% if can? :show, Account # admins + managers %>
        <li>
          <a href="/#/admin/account"><i class="fa fa-cog"></i> Account</a>
        </li>
      <% end %>
        <li>
          <a href="/#/profile"><i class="fa fa-user"></i> Profile</a>
        </li>
        <li>
          <a data-method="delete" href="/sign_out"><i class="fa fa-sign-out"></i> Sign out</a>
        </li>
      </ul>
    </li>
  </ul>