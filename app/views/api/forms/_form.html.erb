<%= form_for(@api_form) do |f| %>
  <% if @api_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_form.errors.count, "error") %> prohibited this api_form from being saved:</h2>

      <ul>
      <% @api_form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br>
    <%= f.number_field :account_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
